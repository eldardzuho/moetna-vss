---
slug: /references/medusa-workflows/steps/waitConfirmationProductImportStep
sidebar_label: waitConfirmationProductImportStep
tags:
  - step
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# waitConfirmationProductImportStep - Medusa Core Workflows Reference

This documentation provides a reference to the `waitConfirmationProductImportStep`. It belongs to the `@moetnavss/medusa/core-flows` package.

This step waits until a product import is confirmed. It's useful before executing the
batchProductsWorkflow.

This step is asynchronous and will make the workflow using it a [Long-Running Workflow](https://docs.medusajs.com/learn/fundamentals/workflows/long-running-workflow).

```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@moetnavss/framework/workflows-sdk"
import { waitConfirmationProductImportStep } from "@moetnavss/medusa/core-flows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const data = waitConfirmationProductImportStep()
  }
)
```
